# Copy this file to .env and adjust values for your machine.
# .env is loaded automatically by config/config.php — no restart needed
# beyond reloading the page. Never commit the real .env file.

APP_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=sathiwelding
DB_USER=root
DB_PASS=
